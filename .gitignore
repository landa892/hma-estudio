node_modules/
.agents/
.claude/skills/
skills-lock.json

# Nunca deben entrar al repositorio: hoy no hay ninguno, y esto es para que
# siga siendo asi. Las claves van en las variables de entorno de Vercel.
.env
.env.*
*.pem
*.key

# Basura de Python y notas de trabajo del generador del espejo.
__pycache__/
docs/en_faltantes.json
.claude/

# Configuracion de herramientas locales. Trae claves de API en texto plano
# y no tiene que salir nunca del equipo.
.codex/
.codex-tmp/

# La conexion del panel a Supabase. Los dos valores que trae son publicos por
# diseño -viajan al navegador en cada visita y lo que protege los datos es el
# RLS de la base-, pero el archivo igual no va al repo: asi el codigo no queda
# atado a una cuenta y transferir el proyecto al estudio es cambiar dos
# variables. Lo escribe docs/panel_config.py en cada build.
admin/config.js
